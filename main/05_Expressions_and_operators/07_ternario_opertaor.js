/*
operador condicional (ternario)
Es el único operador de JavaScript que usa 3 operandos.
Sirve para tomar una decisión y devolver uno de dos valores.
Es una forma corta de escribir un if...else. 
*/

const estado = edad >= 18 ? "adulto" : "menor";